name: Build and Deploy to cPanel

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: '20'

jobs:
  build:
    name: Build + Package for cPanel
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Update browserslist database
        run: npx update-browserslist-db@latest --yes

      - name: Build project
        run: npm run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}

      - name: Create .htaccess for SPA routing
        run: |
          cat > dist/.htaccess << 'EOF'
          # engineersTech - SPA Routing & Optimization
          # Generated by GitHub Actions

          # Enable rewrite engine
          RewriteEngine On
          RewriteBase /

          # Force HTTPS
          RewriteCond %{HTTPS} off
          RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

          # Remove www (optional - uncomment if needed)
          # RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC]
          # RewriteRule ^(.*)$ https://%1/$1 [R=301,L]

          # Handle SPA routing - serve index.html for all non-file requests
          RewriteCond %{REQUEST_FILENAME} !-f
          RewriteCond %{REQUEST_FILENAME} !-d
          RewriteRule ^ index.html [QSA,L]

          # Security Headers
          <IfModule mod_headers.c>
            # Prevent MIME type sniffing
            Header set X-Content-Type-Options "nosniff"
            
            # XSS Protection
            Header set X-XSS-Protection "1; mode=block"
            
            # Clickjacking protection
            Header set X-Frame-Options "SAMEORIGIN"
            
            # Referrer Policy
            Header set Referrer-Policy "strict-origin-when-cross-origin"
            
            # Permissions Policy
            Header set Permissions-Policy "geolocation=(), microphone=(), camera=()"
          </IfModule>

          # Caching for static assets
          <IfModule mod_expires.c>
            ExpiresActive On
            
            # Images
            ExpiresByType image/jpeg "access plus 1 year"
            ExpiresByType image/png "access plus 1 year"
            ExpiresByType image/gif "access plus 1 year"
            ExpiresByType image/webp "access plus 1 year"
            ExpiresByType image/svg+xml "access plus 1 year"
            ExpiresByType image/x-icon "access plus 1 year"
            
            # Fonts
            ExpiresByType font/woff2 "access plus 1 year"
            ExpiresByType font/woff "access plus 1 year"
            ExpiresByType font/ttf "access plus 1 year"
            
            # CSS and JavaScript
            ExpiresByType text/css "access plus 1 year"
            ExpiresByType application/javascript "access plus 1 year"
            ExpiresByType text/javascript "access plus 1 year"
            
            # HTML - short cache for dynamic content
            ExpiresByType text/html "access plus 0 seconds"
          </IfModule>

          # Compression
          <IfModule mod_deflate.c>
            AddOutputFilterByType DEFLATE text/plain
            AddOutputFilterByType DEFLATE text/html
            AddOutputFilterByType DEFLATE text/xml
            AddOutputFilterByType DEFLATE text/css
            AddOutputFilterByType DEFLATE application/xml
            AddOutputFilterByType DEFLATE application/xhtml+xml
            AddOutputFilterByType DEFLATE application/rss+xml
            AddOutputFilterByType DEFLATE application/javascript
            AddOutputFilterByType DEFLATE application/x-javascript
            AddOutputFilterByType DEFLATE application/json
            AddOutputFilterByType DEFLATE image/svg+xml
          </IfModule>

          # Disable directory browsing
          Options -Indexes

          # Block access to sensitive files
          <FilesMatch "(^\.htaccess|\.env|\.git|composer\.(json|lock)|package(-lock)?\.json)">
            Order allow,deny
            Deny from all
          </FilesMatch>

          # Custom error pages
          ErrorDocument 404 /index.html
          EOF

      - name: Create deployment ZIP
        run: |
          cd dist
          zip -r9 ../deploy-cpanel.zip .
          cd ..
          echo "Created deploy-cpanel.zip"
          ls -lh deploy-cpanel.zip

      - name: Upload cPanel deployment package
        uses: actions/upload-artifact@v4
        with:
          name: deploy-cpanel
          path: deploy-cpanel.zip
          retention-days: 30

      - name: Build Summary
        run: |
          echo "## 🚀 Deployment Package Ready" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Artifact: \`deploy-cpanel.zip\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📋 Deployment Steps:" >> $GITHUB_STEP_SUMMARY
          echo "1. Download \`deploy-cpanel\` artifact from Actions tab" >> $GITHUB_STEP_SUMMARY
          echo "2. Login to cPanel → File Manager" >> $GITHUB_STEP_SUMMARY
          echo "3. Navigate to \`public_html\` folder" >> $GITHUB_STEP_SUMMARY
          echo "4. Upload \`deploy-cpanel.zip\`" >> $GITHUB_STEP_SUMMARY
          echo "5. Right-click → Extract → Extract files" >> $GITHUB_STEP_SUMMARY
          echo "6. Delete the ZIP file after extraction" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### ✅ Included:" >> $GITHUB_STEP_SUMMARY
          echo "- Production build with optimized assets" >> $GITHUB_STEP_SUMMARY
          echo "- .htaccess with SPA routing, HTTPS redirect, caching" >> $GITHUB_STEP_SUMMARY
          echo "- Security headers configured" >> $GITHUB_STEP_SUMMARY
          echo "- Gzip compression enabled" >> $GITHUB_STEP_SUMMARY
